
accounts: Google,Facebook,/Email/

user: {
    id[mongoDB ID / primary key],
    name[String(optional)],
    email[String],
    password[String(encrypted)],
    totalNumberOfPosts[int],
    totalNumberOfDrafts[int],
    lastLoggedIn[timestamp],
    listOfBookmarkedPosts[list of bookmarked post id's],
    lastPostPosted[timestamp],
    totalPostViewed[int],
    totalPostLiked[int],
    listOfLikedPosts[list of liked post id's],
    listOfPosts[list of user's created post id's],
}

path = /login {
    email[String],
    password[String(encrypted)],
}// then send verification email

path = /signup{
    name[String],
    email[String],
    password[String[encrypted]],
    profileImage[BitMap],
}

post:{
    postID[int],
    authorID,
    authorName,
    authorEmail,
    postTitle,
    postImage,
    postDesc,
    postContent,
    totalNumberOfLikes,
    totalNumberOfShares,
}